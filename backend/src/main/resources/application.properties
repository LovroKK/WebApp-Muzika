#Define
spring.application.name=beatsync-backend

# MySQL Connection
spring.datasource.url=jdbc:mysql://localhost:3306/webapp_muzika?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Multipart file upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=12MB

# JWT
app.jwt.secret=nekiDugacakTajniKljucMin32Karaktera123!
app.jwt.expiration-ms=28800000

# Admin credentials
admin.username=admin
admin.password=$2a$12$vQHf2/dBwg6U7UX.W79W6eH8HaZ/1dBjvR3VnHXrrPqp5E24H7cd.

# Server
server.port=8080

# Stripe (TEST mode)
# API kljuc sa https://dashboard.stripe.com/test/apikeys
# Webhook secret iz stripe CLI output-a:
# stripe listen --forward-to localhost:8080/api/placanja/webhook
# Maknuti keyeve prije pushanja na dev/main branch, ili dodati u gitignore.
stripe.secret.key=
stripe.webhook.secret=
stripe.success.url=http://127.0.0.1:5500/frontend/payment-success.html
stripe.cancel.url=http://127.0.0.1:5500/frontend/payment-cancel.html

# Logging
logging.level.hr.beatsync=DEBUG